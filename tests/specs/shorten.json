[
  {
    "name": "Shorten asks for a source summary when none exists",
    "steps": [
      {
        "send": { "callback": "summary:shorten" },
        "expect": [{ "method": "editMessageText", "payload": { "text": "No summary yet — send a study file first." } }]
      }
    ]
  }
]
