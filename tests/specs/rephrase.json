[
  {
    "name": "Rephrase asks for a source summary when none exists",
    "steps": [
      {
        "send": { "callback": "summary:rephrase" },
        "expect": [{ "method": "editMessageText", "payload": { "text": "No summary yet — send a study file first." } }]
      }
    ]
  }
]
