[
  {
    "name": "Expand asks for a source summary when none exists",
    "steps": [
      {
        "send": { "callback": "summary:expand" },
        "expect": [{ "method": "editMessageText", "payload": { "text": "No summary yet — send a study file first." } }]
      }
    ]
  }
]
